resources:
  - name: my_docker_repo
    type: GitRepo
    configuration: 
      path: ajit-jain/docker-demo
      gitProvider: myGit

pipelines:
  - name: my_demo_pipeline
    steps: 
      - name: bash_demo
        type: Bash
        execution:
          onStart:
            - echo "Preparing for work..."
          onExecute:
            - echo "$(printenv)"
            - cd /var/opt/jfrog/pipelines/data/my_demo_pipeline/runs/5/steps/bash_demo/$run_number/dependencyState/resources/my_docker_repo
            - ls
        configuration: 
          inputResources: 
            - name: my_docker_repo
          integrations: 
            - name: docker_hub
          
